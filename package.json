{
  "name": "electrification-ev-planning-agent",
  "version": "0.1.0",
  "private": true,
  "description": "MVP dashboard for EV charging infrastructure planning and electrification rollout.",
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev-server.mjs",
    "check": "npm test && node scripts/smoke-test.mjs",
    "test": "node tests/run-tests.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
